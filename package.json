{
  "name": "flair-freeagent",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "predeploy:freeagent": "npm run lint && npm ls",
    "deploy:auth": "gcloud functions deploy authCallback     --runtime nodejs20 --trigger-http --region=europe-west2 --allow-unauthenticated --entry-point authCallback --set-env-vars=GCP_PROJECT=flair-freeagent",
    "deploy:dashboard": "gcloud functions deploy adminDashboard --runtime nodejs20 --trigger-http --region=europe-west2 --entry-point=adminDashboard",
    "deploy:freeagent": "npm run predeploy:freeagent && gcloud functions deploy FreeAgent --runtime=nodejs20 --trigger-http --region=europe-west2 --allow-unauthenticated --source=. --entry-point=FreeAgent --gen2"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "@google-cloud/logging": "^11.2.0",
    "@google-cloud/secret-manager": "^4.2.0",
    "google-auth-library": "^9.15.1",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "eslint": "^9.25.1",
    "globals": "^16.0.0"
  }
}
