{
  "name": "flair-auth",
  "version": "1.0.0",
  "description": "FreeAgent OAuth handler with Firestore and Secret Manager",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=authCallback",
    "deploy": "gcloud functions deploy FreeAgent --runtime=nodejs20 --trigger-http --region=europe-west2 --allow-unauthenticated --source=. && gcloud functions deploy adminDashboard --runtime nodejs20 --trigger-http --region europe-west2 --allow-unauthenticated --entry-point=adminDashboard"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "@google-cloud/functions-framework": "^3.5.1",
    "@google-cloud/secret-manager": "^4.2.0",
    "node-fetch": "^2.6.7"
  }
}